export declare enum TradingPartnerStatus {
    ACCEPTED = "accepted",
    REJECTED = "rejected",
    CANCELED = "canceled",
    PENDING = "pending"
}
export declare enum TradingPartnerType {
    DIRECT = "direct",
    AGENCY = "agency"
}
export interface ITradingPartner {
    checkBalance(currency: string, amount: string): Promise<boolean>;
}
//# sourceMappingURL=iTradingPartner.d.ts.map